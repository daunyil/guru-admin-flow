{
  "name": "guru-admin-flow",
  "version": "1.0.0",
  "private": true,
  "description": "Asisten administrasi guru — local-first PWA v1.0 stable.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @guru-admin/teacher-admin",
    "build": "npm run typecheck --workspaces --if-present && npm run build --workspace @guru-admin/teacher-admin",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "echo 'lint TBD Sprint 2'",
    "test": "npm run test:run --workspaces --if-present",
    "test:run": "npm run test:run --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
